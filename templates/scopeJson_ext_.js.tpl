var dataScope=angular.module('tmDataScope',[]);
dataScope.factory('sData',function(){
    var root={};
    <!-- {line#0} -->
    root.headerStyle={$remark#0};
    <!-- {/line} -->
    return root;
});